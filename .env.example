# ==============================================================================
# FYDBACK ENVIRONMENT VARIABLES - EXAMPLE
# ==============================================================================
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
# 
# Then edit .env.local with your actual API keys.
# ==============================================================================

# ------------------------------------------------------------------------------
# SERVER-SIDE SECRETS (Never exposed to browser)
# ------------------------------------------------------------------------------
# ⚠️  These are used in API routes (/src/app/api/**/route.ts)
# ⚠️  NEVER commit these with real values - use .env.local

# Ultravox API Key - Get from: https://ultravox.ai/dashboard
ULTRAVOX_API_KEY=your-ultravox-api-key-here

# Backend API Key - Your custom backend authentication key
BACKEND_API_KEY=your-backend-api-key-here

# Backend API URL - Your backend service endpoint
BACKEND_URL=https://ultravox-voice-agent.fly.dev/api


# ------------------------------------------------------------------------------
# CLIENT-SIDE PUBLIC VARIABLES (Exposed to browser)
# ------------------------------------------------------------------------------
# These are prefixed with NEXT_PUBLIC_ and embedded in the client bundle.

# Ultravox Agent ID - Your specific voice agent
NEXT_PUBLIC_ULTRAVOX_AGENT_ID=your-agent-id-here

# Daily call limit for demo purposes
NEXT_PUBLIC_ULTRAVOX_DAILY_CALL_LIMIT=50


# ==============================================================================
# DEPLOYMENT (Vercel)
# ==============================================================================
# Add these in Vercel Dashboard → Settings → Environment Variables:
# - ULTRAVOX_API_KEY (Production value)
# - BACKEND_API_KEY (Production value)
# - BACKEND_URL (Production value)
# - NEXT_PUBLIC_ULTRAVOX_AGENT_ID (Production value)
# - NEXT_PUBLIC_ULTRAVOX_DAILY_CALL_LIMIT (Production value)
# ==============================================================================
