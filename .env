# ==============================================================================
# FYDBACK ENVIRONMENT VARIABLES
# ==============================================================================
# This file contains DEFAULT/PLACEHOLDER values and is safe to commit to Git.
# 
# For local development, create a .env.local file (NOT committed) with your 
# actual API keys. Values in .env.local will override these defaults.
# ==============================================================================

# ------------------------------------------------------------------------------
# SERVER-SIDE SECRETS (Never exposed to browser)
# ------------------------------------------------------------------------------
# ⚠️  DO NOT put real API keys here! Use .env.local instead.
# These are used in API routes (/src/app/api/**/route.ts)

# Ultravox API Key - Get from: https://ultravox.ai/dashboard
ULTRAVOX_API_KEY=your-ultravox-api-key-here

# Backend API Key - Your custom backend authentication key
BACKEND_API_KEY=your-backend-api-key-here

# Backend API URL - Your backend service endpoint
BACKEND_URL=https://ultravox-voice-agent.fly.dev/api


# ------------------------------------------------------------------------------
# CLIENT-SIDE PUBLIC VARIABLES (Exposed to browser)
# ------------------------------------------------------------------------------
# These are prefixed with NEXT_PUBLIC_ and are embedded in the client bundle.
# It's safe to commit these as they're meant to be public.

# Ultravox Agent ID - Your specific voice agent
NEXT_PUBLIC_ULTRAVOX_AGENT_ID=1fbf806f-949b-467c-8092-3b85b9f143a5

# Daily call limit for demo purposes
NEXT_PUBLIC_ULTRAVOX_DAILY_CALL_LIMIT=50


# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 
# 1. Copy this file to .env.local:
#    cp .env .env.local
# 
# 2. Edit .env.local and add your real API keys
# 
# 3. Never commit .env.local to Git (it's already in .gitignore)
# 
# 4. For production (Vercel):
#    - Add environment variables in Vercel Dashboard
#    - Settings → Environment Variables
#    - Add: ULTRAVOX_API_KEY, BACKEND_API_KEY, BACKEND_URL
#    - Public variables (NEXT_PUBLIC_*) can be added there too
# 
# ==============================================================================
